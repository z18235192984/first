$ git init
$ git add . 添加所有文件
$ git commit -m "注释" 提交
$ git push 提交
$ git pull 更新
$ git pull origin master 
$ git sataus 状态




Spring 计时 @Component 在类上面写
@Scheduled计时("0 0 0 0 0 0 0")
按顺序依次为
秒（0~59）
分钟（0~59）
小时（0~23）
天（月）（0~31，但是你需要考虑你月的天数）
月（0~11）
天（星期）（1~7 1=SUN 或 SUN，MON，TUE，WED，THU，FRI，SAT）
7.年份（1970－2099）
